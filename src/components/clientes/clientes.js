import Header from '../header/header';
import React from 'react'

const Clientes = (props) => {
    let content = (
        <div>
            <Header/>
        </div>
    )
    return content;
}


export default Clientes;
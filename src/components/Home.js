import Header from './header/header';

import React from 'react'

const Home = (props) => {
    let content = (
        <div>
            <Header/>
        </div>
    )
    return content;
}


export default Home;